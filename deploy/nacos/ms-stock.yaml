# ms-stock 共享配置（Nacos 配置中心 dataId：ms-stock.yaml，group=DEFAULT_GROUP）
# 同步源：本文件 —— 改这里后推送到 Nacos；或在 Nacos 控制台改后同步回本文件
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    banner: false